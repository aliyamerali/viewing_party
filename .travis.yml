language: ruby
rbenv:
- 2.7.2
addons:
  postgresql: 9.6
script:
- bundle exec rails db:{create,migrate} RAILS_ENV=test
- bundle exec rspec
deploy:
  provider: heroku
  api_key: $AM_HEROKU_SECURE_KEY
  app: glacial-scrubland-24016
  on:
    repo: aliyamerali/viewing_party
    branch: am-tinkering
  skip_cleanup: 'true'
